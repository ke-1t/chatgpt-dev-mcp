# Third-party notices

## xyTom/coding-tools-mcp

This project depends on and invokes [`xyTom/coding-tools-mcp`](https://github.com/xyTom/coding-tools-mcp) at runtime. It is an independent MIT-licensed wrapper project: it contains no upstream source code, does not vendor or modify the upstream package, and consumes it solely as a separately distributed PyPI dependency pinned at `coding-tools-mcp==0.3.0`.

The upstream project is licensed under the Apache License 2.0. The installed package and its `LICENSE`/`NOTICE` files remain the source of truth for that dependency's licensing; Apache-2.0 terms apply to the `coding_tools_mcp` package as distributed by upstream, not to this repository's code.

## chatgpt-dev-mcp wrapper code

The wrapper code in this repository is MIT-licensed. See [LICENSE](LICENSE) and the `license` field in `pyproject.toml`.
